#include "main.h"
/**
 * mul - multiplies two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the result of multiplying a and b
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
