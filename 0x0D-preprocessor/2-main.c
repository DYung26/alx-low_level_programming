#include <stdio.h>
/**
 * main - The main function of the program.
 * This function prints the name of the current source file
 * using the __FILE__ macro.
 * Return: Always returns 0 to indicate successful execution.
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
