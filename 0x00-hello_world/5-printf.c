#include <stdio.h>
/**
 * main -This function prints a message to the stdout.
 * Return: It returns 0, indicating successful execution.
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return(0);
}
